language: cpp

compiler:
  - gcc

before_install:
  - sudo apt-get install -qq libsdl1.2-dev libsdl-mixer1.2-dev libsdl-ttf2.0-dev


before_script:
  - mkdir build
  - cd build
  - cmake ..
  
script: make
